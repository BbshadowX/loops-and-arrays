const array1 = [1, 2, 3];
// create a array with the a set of number
let reverse = [];
// Create a empty array for the reverse array
for (let i = 0; i < array1.length; i++) {
  reverse.unshift(array1[i]);
  // Create a for statment that takes each number in the first array and adds it the the front of the new array
}
console.log(reverse);
// print the new array
